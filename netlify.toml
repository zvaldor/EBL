[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA fallback — all routes serve index.html, React Router handles navigation
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
